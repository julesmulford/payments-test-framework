{
  "name": "@payments-framework/kafka-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --forceExit --runInBand",
    "test:watch": "jest --watch --runInBand"
  },
  "dependencies": {
    "kafkajs": "^2.2.4",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testTimeout": 45000,
    "testMatch": ["**/*.kafka.spec.ts"],
    "runInBand": true
  }
}
